<h1>OPAAAAAAAAA</h1>

<!-- ================= KORPA ================= -->
<section class="mb-5">
    <h4 class="mb-3">🔥 Knjige iz korpe:</h4>

    <div class="row g-4">
        <% let imaUKorpi=false; %>

            <% cartstatus.forEach(item=> { %>
                <% if (item.status==='U_KORPI' ) { %>
                    <% imaUKorpi=true; %>

                        <% const book=BooksFromCart.find( b=> b.id === item.book_id
                            );
                            %>

                            <% if (book) { %>
                                <div class="col-md-6 col-lg-4" id="book_<%= book.id %>">
                                    <div class="card h-100 shadow-sm">
                                        <div class="card-body">

                                            <h5 class="card-title">
                                                <%= book.title %>
                                            </h5>
                                            <p class="text-muted">
                                                <%= book.author %>
                                            </p>

                                            <p class="fw-bold text-success">
                                                <%= book.price %> KM
                                            </p>

                                            <p>
                                                <span class="badge bg-info">
                                                    <%= book.genre || 'Žanr' %>
                                                </span>
                                                <span class="badge bg-success">
                                                    U_KORPI
                                                </span>
                                                <br>
                                                <strong>Jezik:</strong>
                                                <%= book.language || '—' %>
                                            </p>

                                            <!-- Dugme IZBACI -->
                                            <button class="btn btn-danger" onclick="izbaci(<%= book.id %>)">
                                                Izbaci
                                            </button>

                                        </div>
                                    </div>
                                </div>
                                <% } %>
                                    <% } %>
                                        <% }) %>
    </div>

    <!-- Dugme NARUCI ide SAMO na kraj sekcije -->
    <% if (imaUKorpi) { %>
        <div class="mt-4">
            <button class="btn btn-primary" onclick="naruci()">
                Naruci
            </button>
        </div>
        <% } %>
</section>


<!-- ================= NARUČENE ================= -->
<section class="mb-5">
    <h4 class="mb-3">📦 Naručene knjige:</h4>

    <div class="row g-4">
        <% let imaNarucene=false; %>

            <% cartstatus.forEach(item=> { %>
                <% if (item.status==='NARUCENO' ) { %>
                    <% imaNarucene=true; %>

                        <% const book=BooksFromCart.find( b=> b.id === item.book_id);    %>

                            <% if (book) { %>
                                <div class="col-md-6 col-lg-4" id="book_<%= book.id %>">
                                    <div class="card h-100 shadow-sm border-success">
                                        <div class="card-body">

                                            <h5 class="card-title">
                                                <%= book.title %>
                                            </h5>
                                            <p class="text-muted">
                                                <%= book.author %>
                                            </p>

                                            <p class="fw-bold text-success">
                                                <%= book.price %> KM
                                            </p>

                                            <span class="badge bg-success">
                                                NARUČENO
                                            </span>

                                            <!-- Dugme OTKAZI (dummy) -->
                                            <div class="mt-3">
                                                <button class="btn btn-secondary" onclick="izbaci(<%= book.id %>)">
                                                    Otkaži narudžbu
                                                </button>
                                            </div>
                                            <p class="badge bg-warning text-dark">
                                                ⏳ Na čekanju
                                            </p>


                                        </div>

                                    </div>
                                </div>
                                <% } %>
                                    <% } %>
                                        <% }) %>
    </div>

    <% if (!imaNarucene) { %>
        <p class="text-muted">Nema naručenih knjiga.</p>
        <% } %>
</section>

<p> <STRONG> URADI JOS I OSTVARNEE NARUDJBE KOJE SU KUPLJENJE ILI OTKAZANE </STRING></p>